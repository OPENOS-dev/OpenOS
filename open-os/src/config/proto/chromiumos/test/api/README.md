# Regenerating go and python files for local development in chroot

```
cros_workon --host start cros-config-api && sudo emerge cros-config-api
```

# Regenerating files for submit (or infra development)

```
../../../../generate.sh
```
