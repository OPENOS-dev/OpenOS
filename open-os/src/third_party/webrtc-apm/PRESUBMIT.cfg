[Hook Scripts]
cros format: cros format --include=webrtc_apm/* --exclude=* --check --commit ${PRESUBMIT_COMMIT} -- ${PRESUBMIT_FILES}

[Hook Overrides Options]
cros_license_check:
    --exclude_regex=^api/
    --exclude_regex=^audio/
    --exclude_regex=^common_audio/
    --exclude_regex=^experiments/
    --exclude_regex=^modules/
    --exclude_regex=^rtc_base/
    --exclude_regex=^rtc_tools/
    --exclude_regex=^system_wrappers/
    --exclude_regex=^third_party/
