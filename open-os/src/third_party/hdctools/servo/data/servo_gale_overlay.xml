<root>
  <include>
    <name>servoflex_cosmos.xml</name>
  </include>
  <control>
    <name>dev_mode</name>
    <doc>dev_mode GPIO is inverted on gale</doc>
    <params map="onoff_i" clobber_ok="patch"></params>
  </control>
  <control>
    <name>lid_open</name>
    <doc>fake lid for products without one</doc>
    <params interface="servo" drv="na" init="not_applicable"
    clobber_ok="full"></params>
  </control>
  <control>
    <name>power_state</name>
    <doc>Used to turn the DUT off and on</doc>
    <params cmd="set" interface="servo" drv="storm_power"
    input_type="str" />
  </control>
  <control>
    <name>cold_reset</name>
    <alias>warm_reset</alias>
    <doc>warm_reset not connected on Gale flex cable. Connect it to
    cold_reset.</doc>
    <params clobber_ok="patch" />
  </control>
  <!-- gale does not have a CrOS EC (thus also does not support
    communication through it).-->
  <control>
    <name>supports_cros_ec_communication</name>
    <params clobber_ok="patch" value="no"/>
  </control>
</root>
