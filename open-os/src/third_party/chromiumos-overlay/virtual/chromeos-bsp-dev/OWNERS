# This package should rarely change, and exists more to provide overall build
# structure than defining the OS itself as the main contents live in the
# respective virtual/chromeos-bsp* overlay packages instead.
include chromiumos/owners:v1:/infra/OWNERS.build
