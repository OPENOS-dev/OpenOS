This package is used in release for the purpose of shipping audio codecs, and
is distinguished from the FFmpeg package under media-video used in testing.

For security reasons and size concerns in rootfs, we configure the minimal
build that supports the use case.
