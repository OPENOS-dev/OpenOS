[Hook Scripts]
cros format = cros format --check --commit ${PRESUBMIT_COMMIT} --include "*.gn" --include "*.gni" --include "*.md" --include "*.py" --include "*.star" --exclude "*" ${PRESUBMIT_FILES}
cros lint = cros lint --commit ${PRESUBMIT_COMMIT} ${PRESUBMIT_FILES}
