[package]
name = "debug_logger"
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause"

[dependencies]
log = "0.4.14"
rtt-target = { version = "0.3.1", features = ["cortex-m"], optional = true }

[features]
# Enable this feature in order to get debug logs.
dev = [ "rtt-target" ]
