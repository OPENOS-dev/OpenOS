[package]
name = "stm32_bootloader_client"
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause"

[dependencies]
embedded-hal = "0.2.6"
defmt = { version = "0.2.1", optional = true }
log = { version = "0.4.14", optional = true }

[dev-dependencies]
embedded-hal-mock = "0.8.0"

[features]
std = []
