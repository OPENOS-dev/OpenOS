[package]
name = "libstage0"
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause"

[dependencies]
ed25519-compact = { version = "1", default-features = false, features = ["opt_size"] }
mcu_common = { path = "../mcu_common" }

[dev-dependencies]
sign-rom = { path = "../sign-rom" }
