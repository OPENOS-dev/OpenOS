[workspace]
members = [
  "spdm-core",
  "spdm-proc-macros",
  "spdm-requester",
  "spdm-responder-ffi",
  "spdm-responder",
  "spdm-types",
  "test-deps",
  "tests",
]
default-members = [
  "spdm-core",
  "spdm-proc-macros",
  "spdm-requester",
  "spdm-responder-ffi",
  "spdm-responder",
  "spdm-types",
  "tests",
]

resolver = "2"

[profile.release]
strip = true
opt-level = "z"
lto = true
panic = "abort"
