From 43e67ca899c02bc54903644442fa18948f89ad8b Mon Sep 17 00:00:00 2001
From: Jeff Lin <jeffulin@google.com>
Date: Tue, 20 Jan 2026 09:03:36 +0000
Subject: [PATCH] Revert "Remove UNSAFE_BUFFERS use from string_view_util.h"

This reverts commit 67173bf8c1bcd363f365ccb95762373afb2e7a15.
---
 base/strings/string_view_util.h | 12 +++++++++---
 1 file changed, 9 insertions(+), 3 deletions(-)

diff --git a/base/strings/string_view_util.h b/base/strings/string_view_util.h
index 5959cdbd91..2783328565 100644
--- a/base/strings/string_view_util.h
+++ b/base/strings/string_view_util.h
@@ -28,17 +28,23 @@ constexpr std::basic_string_view<CharT> MakeStringViewWithNulChars(
 // std:: has no direct equivalent for this; however, it eases span adoption in
 // Chromium, which uses `string`s and `string_view`s in many cases that
 // rightfully should be containers of `uint8_t`.
+//
+// TODO(C++23): Replace with direct use of the `std::string_view` range
+// constructor to avoid marking UNSAFE_BUFFERS.
 constexpr auto as_string_view(span<const char> s LIFETIME_BOUND) {
-  return std::string_view(s);
+  // SAFETY: legitimate spans are valid from `begin()` to `end()`.
+  return UNSAFE_BUFFERS(std::string_view(s.begin(), s.end()));
 }
 constexpr auto as_string_view(span<const unsigned char> s LIFETIME_BOUND) {
   return as_string_view(as_chars(s));
 }
 constexpr auto as_string_view(span<const char16_t> s LIFETIME_BOUND) {
-  return std::u16string_view(s);
+  // SAFETY: legitimate spans are valid from `begin()` to `end()`.
+  return UNSAFE_BUFFERS(std::u16string_view(s.begin(), s.end()));
 }
 constexpr auto as_string_view(span<const wchar_t> s LIFETIME_BOUND) {
-  return std::wstring_view(s);
+  // SAFETY: legitimate spans are valid from `begin()` to `end()`.
+  return UNSAFE_BUFFERS(std::wstring_view(s.begin(), s.end()));
 }
 
 }  // namespace base
-- 
2.52.0.457.g6b5491de43-goog

