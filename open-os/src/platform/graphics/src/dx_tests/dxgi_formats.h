// Copyright 2023 The ChromiumOS Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

#pragma once

#include <d3d9.h>
#include <dxgi.h>
#include <stdint.h>

template <DXGI_FORMAT x = DXGI_FORMAT_UNKNOWN>
struct format_to_type {};

#define SET_DXGI_TYPE(FORMAT, ELEMENTS, TYPE)      \
  template <>                                      \
  struct format_to_type<FORMAT> {                  \
    using type = TYPE;                             \
    static const uint32_t num_elements = ELEMENTS; \
  }

SET_DXGI_TYPE(DXGI_FORMAT_R32G32B32A32_TYPELESS, 4, uint32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32B32A32_FLOAT, 4, float);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32B32A32_UINT, 4, uint32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32B32A32_SINT, 4, int32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32B32_TYPELESS, 3, uint32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32B32_FLOAT, 3, float);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32B32_UINT, 3, uint32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32B32_SINT, 3, int32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R16G16B16A16_TYPELESS, 4, uint16_t);
SET_DXGI_TYPE(DXGI_FORMAT_R16G16B16A16_FLOAT, 4, uint16_t);
SET_DXGI_TYPE(DXGI_FORMAT_R16G16B16A16_UNORM, 4, uint16_t);
SET_DXGI_TYPE(DXGI_FORMAT_R16G16B16A16_UINT, 4, uint16_t);
SET_DXGI_TYPE(DXGI_FORMAT_R16G16B16A16_SNORM, 4, int16_t);
SET_DXGI_TYPE(DXGI_FORMAT_R16G16B16A16_SINT, 4, int16_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32_TYPELESS, 2, uint32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32_FLOAT, 2, float);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32_UINT, 2, uint32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32G32_SINT, 2, int32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R8G8B8A8_TYPELESS, 4, uint8_t);
SET_DXGI_TYPE(DXGI_FORMAT_R8G8B8A8_UNORM, 4, uint8_t);
SET_DXGI_TYPE(DXGI_FORMAT_R8G8B8A8_UNORM_SRGB, 4, uint8_t);
SET_DXGI_TYPE(DXGI_FORMAT_R8G8B8A8_UINT, 4, uint8_t);
SET_DXGI_TYPE(DXGI_FORMAT_R8G8B8A8_SNORM, 4, int8_t);
SET_DXGI_TYPE(DXGI_FORMAT_R8G8B8A8_SINT, 4, int8_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32_TYPELESS, 1, uint32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32_FLOAT, 1, float);
SET_DXGI_TYPE(DXGI_FORMAT_R32_UINT, 1, uint32_t);
SET_DXGI_TYPE(DXGI_FORMAT_R32_SINT, 1, int32_t);
SET_DXGI_TYPE(DXGI_FORMAT_D24_UNORM_S8_UINT, 1, uint32_t);

#define DX9_FORMAT_TYPE 0x80000000
#define SET_D3D9_TYPE(FORMAT, ELEMENTS, TYPE) \
  SET_DXGI_TYPE(static_cast<DXGI_FORMAT>(DX9_FORMAT_TYPE | FORMAT), ELEMENTS, TYPE)

SET_D3D9_TYPE(D3DFMT_R8G8B8, 3, uint8_t);
SET_D3D9_TYPE(D3DFMT_A8R8G8B8, 4, uint8_t);
SET_D3D9_TYPE(D3DFMT_X8R8G8B8, 4, uint8_t);
SET_D3D9_TYPE(D3DFMT_A8B8G8R8, 4, uint8_t);
SET_D3D9_TYPE(D3DFMT_X8B8G8R8, 4, uint8_t);

#undef SET_DXGI_TYPE
#undef SET_D3D9_TYPE

#define TO_PSEUDO_DXGI_FORMAT(D3DFORMAT) \
  static_cast<DXGI_FORMAT>(DX9_FORMAT_TYPE | D3DFORMAT)
