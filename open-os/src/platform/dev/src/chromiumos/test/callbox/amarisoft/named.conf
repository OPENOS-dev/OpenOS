// Copyright 2023 The ChromiumOS Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

options {
    listen-on port 53 { 192.168.2.1;192.168.3.1;192.168.4.1;192.168.5.1;192.168.6.1; 192.168.7.1; 192.168.8.1; 192.168.9.1;};
    listen-on-v6 port 53 { 2001:468:3000:1::/64;2001:468:4000:1::/64;2001:468:5000:1::/64;2001:468:6000:1::/64;2001:468:7000:1::/64;2001:468:8000:1::/64;2001:468:9000:1::/64; };
    directory     "/var/named";
    dump-file     "/var/named/data/cache_dump.db";
    statistics-file "/var/named/data/named_stats.txt";
    memstatistics-file "/var/named/data/named_mem_stats.txt";
    secroots-file    "/var/named/data/named.secroots";
    recursing-file    "/var/named/data/named.recursing";
    #allow-query     { localhost;}; # Commented to allow from all hosts

    /*
     - If you are building an AUTHORITATIVE DNS server, do NOT enable recursion.
     - If you are building a RECURSIVE (caching) DNS server, you need to enable
       recursion.
    */
    recursion yes;

    dnssec-validation yes;

    managed-keys-directory "/var/named/dynamic";
    geoip-directory "/usr/share/GeoIP";

    pid-file "/run/named/named.pid";
    session-keyfile "/run/named/session.key";

    forwarders {
        8.8.8.8;
        8.8.4.4;
        2001:4860:4860::8888;
    };

    /* https://fedoraproject.org/wiki/Changes/CryptoPolicy */
    include "/etc/crypto-policies/back-ends/bind.config";
};

logging {
        channel default_debug {
                file "data/named.run";
                # change severity to `debug 3` for verbose logging
                severity dynamic;
        };
};

include "/etc/named.rfc1912.zones";
include "/etc/named.root.key";

include "/etc/cros_named_zones.conf";
