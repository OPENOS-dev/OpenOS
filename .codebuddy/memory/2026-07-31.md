# 2026-07-31

## 构建 — OPT 包管理器 + ebuild 校验

### OPT (opt/) Cargo workspace
- **libopt + opt-cli**: `cargo build` 成功，`cargo fix` 修复 2 warnings
- **剩余 1 warning**: `static_mut_refs`（MANAGER 全局 static mut 单例，设计选择）
- **二进制**: `target/debug/opt` 输出 `opt 0.1.0` ✅

### openos-overlay ebuild 结构校验
- **openos-setup-1.0.0**: EAPI=7，6 个全部 OK，FILESDIR 两个文件均存在
- **openos-meta-1.0.0**: EAPI=7，元包无 src_install 正常
- **openos-theme-1.0.0**: EAPI=7，OK

### 不可构建
- OS 完整镜像：需要 Linux chroot (cros_sdk → build_packages → build_image)
- Go 组件 (opk-server)：macOS 未安装 Go
