# 2026-07-18

## chromite 全面去 ChromiumOS/Google 化（版权头 + 品牌名替换）

用户需求：
- 版权头: `The ChromiumOS Authors` → `The OpenOS Authors`
- 品牌名: `ChromiumOS`/`Chrome OS`/`ChromeOS`/`chromiumos` → `OpenOS`
- 镜像名保留（chromiumos_base_image 等）
- Google 邮箱/URL 全部删除或替换为 example.com
- LICENSE: `Google LLC` → `The OpenOS Authors`

执行结果（全量验证）：
- 旧版权头: 0 残留
- 新版权头: 801 个文件
- 旧品牌名: 0 残留
- Google 邮箱/googlesource/chromium.org: 0 残留（排除 gen 和 third_party）
- 镜像名: 已保护

改动的关键文件：
- `LICENSE`, `AUTHORS`, `DIR_METADATA`, `README.chromium` — 身份文件
- `lib/constants.py` — overlay 路径、SDK bucket、env var、移植版本
- `lib/chromeos_version.py` → `lib/openos_version.py` — 重命名 + 字段改名
- `utils/hostname_util.py` — Google 域名检测清空
- `README.md` — 品牌/URL 替换
- 801 个文件的版权头批量替换

未修改：`third_party/`（第三方）、`api/gen*/`（protobuf Go 标识符 — 功能性代码）

## cros_sdk 多平台支持（见前文摘要）

## 第二轮品牌更新（2026-07-18 续）

用户需求：
- 组织名: OCS (Open Code Studio)
- 系统展示名: OPENOS（代码标识符/变量名保留小写 openos）
- OPK → OPT 改名，规划用 OpenScript 重写
- OpenScript 仓库: /Users/cangcang/code/Open-Script-OS

执行内容：

### 1. 版权头: `The OpenOS Authors` → `OCS (Open Code Studio)`
- chromite 下 809 个文件全部替换（排除 third_party 和 api/gen）
- LICENSE、AUTHORS、README.md 同步更新
- opk/opt 目录下 11 个 .rs/.toml/.cpp/.h/.md 文件同步更新

### 2. OPENOS 展示名替换
- 策略: 保护标识符（OpenOSDevice、OpenOSVersionFinder 等类名）→ 全局替换 → 恢复标识符
- 结果: 177 个文件中的注释/文档字符串/显示字符串替换为 OPENOS
- 代码标识符全部保护（0 误改）

### 3. opk/ → opt/ 目录重命名
- `opk/` → `opt/` 目录重命名完成
- `opk-repos.conf` → `opt-repos.conf`（Overlay 配置文件）
- ebuild 中 `insinto /etc/opk` → `insinto /etc/opt`
- `openos.conf` 中 `config=/etc/opk/opk-repos.conf` → `/etc/opt/opt-repos.conf`
- `openos-base-1.0.0.ebuild` 版权和路径更新
- opt/README.md 重写，添加 OpenScript 迁移计划和命令参考
- README.md 更新结构树和品牌描述

### 4. 记忆文件
- MEMORY.md 完整重写，反映新组织/系统名和 OPT 架构
