# 2026-07-20

## Shell 提示符
- 改 `01-prompt.sh`：hostname 固定为 `OPENOS`（原从 chromeos-config 读设备型号名）
- board_id 追加逻辑保留：`chronos@OPENOS-rev3 ~ $`
- 版权头改为 OCS

## OPENOS 图标
- 用户用 Inkscape 手绘了 "O" 字形 logo（256×256，O 字 + 绿色 #4aa26f 下划线）
- `openos-logo-light.svg` — 黑色 O（浅色背景）
- `openos-logo-dark.svg` — 白色 O（深色背景）
- `openos-logo.svg` — 自适应版，CSS `@media (prefers-color-scheme: dark)` 自动切换
- 创建 ebuild：`x11-themes/openos-icons/openos-icons-1.0.0.ebuild`
- 安装到 `/usr/share/openos/icons/`
- `openos-meta` 已通过 openos-branding USE flag 依赖图标包

## Welcome 脚本
- `openos-welcome.sh` 新增 `--static` 模式：首次启动动画，之后瞬间打印
- 去掉了假提示符（`chronos@openos:~$ `），让真实 shell 自己显示
