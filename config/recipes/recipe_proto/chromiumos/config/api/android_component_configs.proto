// Copyright 2025 The ChromiumOS Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

// This protocol buffer is based off the autogenerated one, converted from
// device/google/desktop/common/config/hal_config.xsd, using Google3 python
// script xsd2proto.
//
// cmdline:
// blaze build //finance/scripts/xsd2proto:xsd2proto &&
// /google3/blaze-bin/finance/scripts/xsd2proto/xsd2proto
// --xsd_file=hal_config.xsd --output_file=hal_config.proto --emit_cdata=false
//
// Message definitions are added one by one.

syntax = "proto3";

package chromiumos.config.api;

import "google/protobuf/descriptor.proto";

option go_package = "go.chromium.org/chromiumos/config/go/api";

extend google.protobuf.FieldOptions {
  // Overrides the default XML node name generated by cros_to_android.py
  // for a specific field. When this option is present, the XML generation
  // script will use this exact string instead of converting the protobuf
  // field name (which normally replaces underscores with hyphens).
  optional string name_override = 50000;
}

// Audio component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
//   config component for all devices under the same project build. The enumer-
//   ration is generated by boxster config infra according to this project's
//   hw_mapping_table.txtpb file under its project code repo.
// soundcard: Internal sound card name.
message AudioConfigurationType {
  string id = 1;
  optional string soundcard = 2;
  optional int32 dmics_count = 3;
  optional string audio_config_dir = 4;
  optional bool default = 5;
}

// Cellular component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
//   config component for all devices under the same project build. The enumer-
//   ration is generated by boxster config infra according to this project's
//   hw_mapping_table.txtpb file under its project code repo.
// firmware_variant: The modem firmware file to flash onto the modem.
// modem_type: One of ModemTypeEn
message CellularConfigurationType {
  string id = 1;
  enum ModemType {
    MODEM_UNKNOWN = 0;
    MODEM_L850 = 1;
    MODEM_NL668 = 2;
    MODEM_FM101 = 3;
    MODEM_FM350 = 4;
    MODEM_SC7180 = 5;
    MODEM_SC7280 = 6;
    MODEM_EM060 = 7;
    MODEM_RW101 = 8;
    MODEM_RW135 = 9;
    MODEM_LCUK54 = 10;
    MODEM_RW350 = 11;
  }
  optional ModemType modem_type = 2;
  optional string firmware_variant = 3;
  optional bool default = 4;
}

// Fingerprint component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
//   config component for all devices under the same project build. The enumer-
//   ration is generated by boxster config infra according to this project's
//   hw_mapping_table.txtpb file under its project code repo.
// board: Specifies the fingerprint board in use.
// fingerprint_sensor_type: Type of FP sensor. Currently describes whether FP is
//   overlapped on the power button or not.
// ro_version: RO version for the fingerprint firmware for the FPMCU specified
//   by the "board" property. If not specified, the default RO version for the
//   FPMCU is used.
// sensor_location: Specifies the location of the fingerprint sensor.
message FingerprintConfigurationType {
  string id = 1;
  string board = 2;
  optional string fingerprint_sensor_type = 3;
  enum Location {
    LOCATION_UNKNOWN = 0;
    // Top of the screen (e.g. Pixel Slate) at the left
    POWER_BUTTON_TOP_LEFT = 1;
    // Bottom of keyboard at the left
    KEYBOARD_BOTTOM_LEFT = 2;
    // Bottom of keyboard at the right
    KEYBOARD_BOTTOM_RIGHT = 3;
    // Top of keyboard at the right (e.g. Galaxy Chromebook)
    KEYBOARD_TOP_RIGHT = 4;
    reserved 5;  // Not to conflict with NOT_PRESENT
    // At the right side
    RIGHT_SIDE = 6;
    // At the left side
    LEFT_SIDE = 7;
    reserved 8;  // Not to conflict with PRESENT
    // Left of power button at top right corner (of the keyboard)
    LEFT_OF_POWER_BUTTON_TOP_RIGHT = 9;
    // On power button at the top right key of the keyboard.
    POWER_BUTTON_TOP_RIGHT_KEY = 10;
    // On power button, on the left edge at the top (detachable device,
    // landscape orientation).
    POWER_BUTTON_LEFT_EDGE_TOP = 11;
  }
  optional Location sensor_location = 4;
  optional string ro_version = 5;
  optional bool default = 6;
}

// Camera component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
//   config component for all devices under the same project build. The enumer-
//   ration is generated by boxster config infra according to this project's
//   hw_mapping_table.txtpb file under its project code repo.
// media_profile_suffix: This is used to set ro.media.xml_variant.profiles,
//   which derives the media_profile.xml file for the given SKU.
message CameraConfigurationType {
  string id = 1;
  optional string media_profile_suffix = 2;
  enum Interface {
    INTERFACE_UNKNOWN = 0;
    INTERFACE_USB = 1;
    INTERFACE_MIPI = 2;
  }
  enum Facing {
    FACING_UNKNOWN = 0;
    FACING_FRONT = 1;
    FACING_BACK = 2;
  }
  message Resolution {
    int32 resolutionx = 1;
    int32 resolutiony = 2;
  }
  message CameraHWConfig {
    optional Interface interface = 1;
    optional Facing position = 2;
    optional HalConfiguration.Present autofocus_support = 3;
    reserved 4, 5;
    reserved "resolutionx", "resolutiony";
    optional HalConfiguration.Present p1080_support = 6;
    repeated Resolution resolutions = 7;
    optional HalConfiguration.Present p4k_support = 8;
    optional HalConfiguration.Present p1440_support = 9;
  }
  repeated CameraHWConfig cameras = 3;
  optional bool default = 4;
}

// Storage component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
//   config component for all devices under the same project build. The enumer-
//   ration is generated by boxster config infra according to this project's
//   hw_mapping_table.txtpb file under its project code repo.
// storage_type: Non-volatile storage type..
message StorageConfigurationType {
  string id = 1;
  optional string storage_type = 2;
  optional bool default = 3;
}

// Keyboard component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
//   config component for all devices under the same project build. The enumer-
//   ration is generated by boxster config infra according to this project's
//   hw_mapping_table.txtpb file under its project code repo.
// backlight_support: Keyboard backlight present.
// kb_default_brightness: Keyboard backlight default brightness.
// kb_backlight_steps: Backlight steps in increasing order.
message KeyboardConfigurationType {
  string id = 1;
  optional HalConfiguration.Present backlight_support = 2;
  optional string kb_default_brightness = 3;
  optional string kb_backlight_steps = 4;
  optional bool default = 5;
}

// Stylus component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
//   config component for all devices under the same project build. The enumer-
//   ration is generated by boxster config infra according to this project's
//   hw_mapping_table.txtpb file under its project code repo.
// stylus_type: Stylus present and garaged type or not.
message StylusConfigurationType {
  string id = 1;
  enum StylusType {
    STYLUS_UNKNOWN = 0;
    NONE = 1;
    INTERNAL = 2;  // Garaged stylus
    EXTERNAL = 3;  // Non-garaged stylus
  }
  optional StylusType stylus_type = 2;
  optional bool default = 3;
}

// Firmware component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message FirmwareConfigurationType {
  string id = 1;
  optional string firmware_manifest_key = 2;
  optional string firmware_config = 3;
  optional string ufsc = 4;
  optional bool default = 5;
}

// Touchscreen component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message TouchscreenConfigurationType {
  string id = 1;
  optional string screen_size = 2;
  optional bool default = 3;
}

// Touchpad component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message TouchpadConfigurationType {
  string id = 1;
  optional bool default = 2;
}

// Video component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message VideoConfigurationType {
  string id = 1;
  optional string video_codec_suffix = 2;
  optional bool default = 3;
}

// HardwareFeatures component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message HardwareFeaturesConfigurationType {
  string id = 1;
  optional string form_factor = 2;
  optional string touchscreen_support = 3;
  optional bool default = 4;
}

// Gyroscope component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message GyroscopeConfigurationType {
  string id = 1;
  optional HalConfiguration.Present feature_gyroscope = 2;
  optional bool default = 3;
}

// Accelerometer component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message AccelerometerConfigurationType {
  string id = 1;
  optional HalConfiguration.Present feature_accelerometer = 2;
  optional bool default = 3;
}

// LightSensor component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message LightSensorConfigurationType {
  string id = 1;
  optional HalConfiguration.Present feature_lightsensor = 2;
  optional bool default = 3;
}

// Magnetometer component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message MagnetometerConfigurationType {
  string id = 1;
  optional HalConfiguration.Present feature_magnetometer = 2;
  optional bool default = 3;
}

// Wifi component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message WifiConfigurationType {
  string id = 1;
  enum WifiChip {
    UNKNOWN = 0;
    INTEL = 1;
    MTK = 2;
    RTW = 3;
    QCOM = 4;
  }
  WifiChip chip = 2 [(name_override) = "Chip"];

  message SarSpecType {
    message PowerConfigType {
      int32 powerlimit = 1 [(name_override) = "PowerLimit"];
      int32 poweroffset = 2 [(name_override) = "PowerOffset"];
    }
    message RegDomainType {
      PowerConfigType powerconfig_2g = 1 [(name_override) = "PowerConfig.2g"];
      PowerConfigType powerconfig_5g = 2 [(name_override) = "PowerConfig.5g"];
      optional PowerConfigType powerconfig_6g = 3
          [(name_override) = "PowerConfig.6g"];
    }
    message PowerTableType {
      PowerConfigType powerconfig_2g = 1 [(name_override) = "PowerConfig.2g"];
      optional PowerConfigType powerconfig_5g = 2
          [(name_override) = "PowerConfig.5g"];
      optional PowerConfigType powerconfig_5g_1 = 3
          [(name_override) = "PowerConfig.5g_1"];
      optional PowerConfigType powerconfig_5g_2 = 4
          [(name_override) = "PowerConfig.5g_2"];
      optional PowerConfigType powerconfig_5g_3 = 5
          [(name_override) = "PowerConfig.5g_3"];
      optional PowerConfigType powerconfig_5g_4 = 6
          [(name_override) = "PowerConfig.5g_4"];
      optional PowerConfigType powerconfig_6g_1 = 7
          [(name_override) = "PowerConfig.6g_1"];
      optional PowerConfigType powerconfig_6g_2 = 8
          [(name_override) = "PowerConfig.6g_2"];
      optional PowerConfigType powerconfig_6g_3 = 9
          [(name_override) = "PowerConfig.6g_3"];
      optional PowerConfigType powerconfig_6g_4 = 10
          [(name_override) = "PowerConfig.6g_4"];
      optional PowerConfigType powerconfig_6g_5 = 11
          [(name_override) = "PowerConfig.6g_5"];
      optional PowerConfigType powerconfig_6g_6 = 12
          [(name_override) = "PowerConfig.6g_6"];
    }
    optional RegDomainType regdomain_fcc = 1
        [(name_override) = "RegDomain.fcc"];
    optional RegDomainType regdomain_eu = 2 [(name_override) = "RegDomain.eu"];
    optional RegDomainType regdomain_other = 3
        [(name_override) = "RegDomain.other"];
    optional PowerTableType powertable_tablet = 4
        [(name_override) = "PowerTable.tablet"];
    optional PowerTableType powertable_clamshell = 5
        [(name_override) = "PowerTable.clamshell"];
  }
  optional SarSpecType mtkconfig = 3 [(name_override) = "MTKConfig"];
  optional SarSpecType rtwconfig = 4 [(name_override) = "RTWConfig"];
  optional HalConfiguration.Present feature_aware = 5;
  optional HalConfiguration.Present feature_direct = 6;
  optional HalConfiguration.Present feature_passport = 7;
  optional HalConfiguration.Present feature_rtt = 8;
  optional bool default = 9;
}

// ProximitySensor component configuration. Annotation to the fields:
// id: The identity field among the enumeration of possible variants on this HW
message ProximityConfigurationType {
  string id = 1;
  message LocationType {
    optional string modifier = 1;
  }
  message ProximityLocationType {
    optional LocationType radio_type_wifi = 1;
    optional LocationType radio_type_cellular = 2;
  }
  message SemtechChannelType {
    optional string channel = 1;
    optional uint32 hardwaregain = 2;
    optional uint32 thresh_falling = 3;
    optional uint32 thresh_falling_hysteresis = 4;
    optional uint32 thresh_rising = 5;
    optional uint32 thresh_rising_hysteresis = 6;
  }
  message SemtechSensorConfigurationType {
    repeated SemtechChannelType channel = 1;
    optional float sampling_frequency = 2;
    optional uint32 thresh_falling_period = 3;
    optional uint32 thresh_rising_period = 4;
  }
  message SemtechProximityConfigurationType {
    optional ProximityLocationType location = 1;
    optional SemtechSensorConfigurationType semtech_config = 2;
  }
  optional SemtechProximityConfigurationType semtech_proximity = 2;
  optional bool default = 3;
}

message HalConfiguration {
  enum Present {
    PRESENT_UNKNOWN = 0;
    PRESENT = 1;
    NOT_PRESENT = 2;
  }

  repeated AudioConfigurationType audio_list = 1;
  repeated FingerprintConfigurationType fingerprint_list = 2;
  repeated CellularConfigurationType cellular_list = 3;
  repeated CameraConfigurationType camera_list = 4;
  repeated StorageConfigurationType storage_list = 5;
  repeated KeyboardConfigurationType keyboard_list = 6;
  repeated StylusConfigurationType stylus_list = 7;
  repeated FirmwareConfigurationType firmware_list = 8;
  repeated TouchscreenConfigurationType touchscreen_list = 9;
  repeated TouchpadConfigurationType touchpad_list = 10;
  repeated VideoConfigurationType video_list = 11;
  repeated HardwareFeaturesConfigurationType hwfeature_list = 12;
  repeated GyroscopeConfigurationType gyroscope_list = 13;
  repeated AccelerometerConfigurationType accelerometer_list = 14;
  repeated LightSensorConfigurationType lightsensor_list = 15;
  repeated MagnetometerConfigurationType magnetometer_list = 16;
  repeated WifiConfigurationType wifi_list = 17;
  repeated ProximityConfigurationType proximity_list = 18;
}
